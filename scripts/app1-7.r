{ rm(list=ls()); 
  options(show.error.location = TRUE);
  
  maxtempToday= as.numeric(readline("what is the max temperature today?"));
  
  if(maxtempToday < 30)
  { 
    cat ("brrrrrrrrrr");
  }
  
  if(maxtempToday >= 80)
  { 
    cat ("enjoy the sunshine");
  }
  weather= readline("what is the weather today"); 
  if(weather == "cloudy")
  {
    cat ("dont worry about sunglasses");
  }
  if(weather == "not rainy")
  {
    cat ("enjoy staying dry");
  } 
  fishLength=fishData$Total_Length_mm
}   
 
 

  
    